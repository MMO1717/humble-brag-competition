# BRAG-Pipeline 技术报告

## 1. 系统目标

BRAG-Pipeline 面向包含隐含炫耀、寻求认可和复杂社交约束的回复任务。系统接收原帖、平台、关系、Agent 角色和互动目标，生成以下七个字段：

```text
episode_id
bragging_mechanism
speaker_intention
desired_feedback
risk_assessment
response_strategy
response_text
```

设计重点不是让模型自由规划整条任务，而是把分类、理解、决策、风险控制和回复生成拆成可审计步骤。模型负责需要语言理解的部分，稳定性要求较高的策略、风险和格式环节由本地规则约束。

## 2. 整体架构

```text
输入数据
  ↓
MechanismSkill
  ↓
UnderstandingSkill
  ↓
StrategySkill
  ↓
RiskSkill
  ↓
ResponseSkill
  ↓
ResponseRiskReviewSkill
  ↓
ValidatorSkill
  ↓
RewriterSkill（仅校验失败时）
  ↓
七字段 JSONL
```

主程序从 `config.py` 派生 smoke、dev 或 test 的数据范围和评估行为。每条样本经过固定 SkillFlow，最终由官方格式检查脚本验证。运行过程同时记录模型原始输出、机制校准、Memory 命中、few-shot 示例、策略分支、回复审查和兜底原因。

## 3. 核心技术

### 3.1 机制分类与校准

MechanismSkill 只把 `speaker_post` 传给分类模型，避免平台、关系或回复目标改变同一原文的机制判断。

模型首先返回：

```text
label
runner_up
confidence
cue_codes
```

本地校准器随后检查高精度信号和常见混淆边界。明确自我标注、稀缺访问等信号可以覆盖模型；宽泛的成就词、比较词或谦虚词不能单独触发覆盖。低置信分类、已知混淆对和规则冲突会触发一次二选一短复核。

复核输出兼容正式机制标签及 `candidate_a/candidate_b` 别名。相同标准化原帖使用运行内缓存，保证跨场景分类一致，并减少重复 API 调用。

### 3.2 社交理解

UnderstandingSkill 根据原帖、场景和最终机制生成简洁的：

- `speaker_intention`
- `desired_feedback`

该步骤允许模型处理开放语义，但输出必须是紧凑 JSON。后处理会清理隐藏推理、异常字段和超长文本。

### 3.3 确定性策略矩阵

StrategySkill 不使用模型投票，而是根据以下信息执行场景矩阵：

- 平台
- 社交关系
- 互动目标
- 最终机制标签

专业场景优先使用克制策略；亲密私聊允许适度认可、追问或轻松调侃；比较优越型表达通常采用重定向；`set_boundary` 和 `no_response` 只在输入明确要求时启用。

公开社交场景还会判断社交温度和继续交流价值，在低风险、非陌生关系和支持性目标下允许 `light_acknowledgment`，避免全部退化为机械的中立观察。

当前版本对首选策略做了更细的局部修正：

- 公开社交媒体中 acquaintance 的比较优越表达优先使用 `neutral_observation`。
- 社区论坛中 stranger 的非说教目标避免过度重定向，优先中立观察。
- 支持性目标下的生日、稀缺访问和私人故事场景优先 `ask_followup`。
- 群聊 acquaintance 的 faux modesty 降低调侃倾向，优先中立观察。

每次决策都会记录命中的规则、依据和最终策略。

### 3.4 精简风险基线

RiskSkill 使用小而稳定的风险集合：

- 默认输出 `misrecognition`
- 正式、公开或关系较远的场景按条件增加 `context_insensitivity`
- 仅在策略与场景明确冲突时使用 `strategy_inconsistency`
- `sycophancy` 只在策略确实偏认可、附和或中立回避时触发，避免把合理追问误判为拍马屁风险

最终通常保留一到两个风险标签，并使用固定模板渲染风险评估。这种设计减少了无依据的风险过预测。

### 3.5 回复生成与审查

ResponseSkill 综合以下信息生成短回复：

- 最终机制和策略
- 意图与期望反馈
- 场景匹配的结构化 Memory
- 从 train 检索的 response few-shot
- 长度、语气、反过誉和反说教约束

回复生成后由 social rubric 和 ResponseRiskReviewSkill 检查：

- 追问策略是否包含真实问题
- 重定向和边界策略是否落实
- 是否包含过誉或说教表达
- 专业、公开和亲密场景的语气是否合适

硬性问题优先使用原帖具体细节构造短回复，再次检查后才允许采用通用模板。回复审查只修正文本并记录 warning，不会扩大最终风险标签集合。

### 3.6 输出安全

ValidatorSkill 检查：

- 七字段是否完整且没有额外字段
- episode ID 是否保持一致
- 机制和策略标签是否合法
- 文本是否为空、超长或包含隐藏推理
- `no_response` 与空回复规则是否一致

首次校验失败时才调用 RewriterSkill。重写仍失败时，系统使用合法的整行兜底输出，确保最终 JSONL 可以通过格式检查。

## 4. Memory 与 Few-shot

### 4.1 结构化 Memory

项目内置 48 条 Memory，统一保存在 `memory/memory.jsonl`：

| 类型 | 数量 | 主要用途 |
|---|---:|---|
| mechanism_rule | 7 | 七类机制知识 |
| confusion_rule | 6 | 机制和策略混淆边界 |
| strategy_policy | 8 | 策略适用范围和表达要求 |
| scenario_policy | 7 | 平台、关系和目标约束 |
| risk_pattern | 6 | 风险识别规则 |
| response_style_card | 8 | 回复策略的风格卡 |
| anti_pattern | 6 | 需要避免的回复模式 |

Memory 使用统一的 11 字段 schema，包含目标 Skill、目标标签、条件、负向条件、置信度、优先级和来源。

机制分类固定加载合法的机制卡和机制混淆卡作为知识背景。ResponseSkill 的检索则先执行 Skill、类型、标签、正向条件和负向条件过滤，再按相关性、置信度和优先级排序。未识别的条件不会被视为命中。

完整 dev 错误分析可以生成与内置 Memory 相同格式的候选，写入 `memory/generated.jsonl`。候选默认不参与推理，只有人工检查后主动启用才会加载。

### 4.2 Few-shot

Few-shot 数据来自 `data/train.jsonl` 的 500 条训练样本。当前稳定配置为：

```python
FEWSHOT_TASK_K = {
    "mechanism": 0,
    "strategy": 0,
    "response": 2,
}
```

机制分类和策略选择不使用近邻投票，避免噪声覆盖机制校准器和策略矩阵。训练集只为 ResponseSkill 提供相似场景中的措辞参考。

检索支持三种模式：

- `jaccard`：本地词法相似度加场景字段权重
- `embedding`：向量语义相似度加场景字段权重
- `hybrid`：语义和词法相似度加权

当前默认使用 Jaccard。它没有额外 API 成本，在多模型完整 dev 中表现稳定。Embedding 作为可选实验功能保留；初始化或请求失败时会自动切换到 Jaccard，并在终端和 manifest 中记录原因。

## 5. 当前测试结果

以下结果来自同一套 45 条公开 dev、Gemma3 12B、Jaccard few-shot。公开 dev 代理分只用于本地迭代，官方排名仍取决于隐藏评估。

| 运行 | Memory 路由 | 机制准确率 | Preferred | Acceptable | 策略得分 | 意图 F1 | 反馈 F1 | 风险 F1 | 回复 F1 | 代理总分 |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| `dev_20260622_001031_gemma3_12b` | function | 0.9778 | 0.7778 | 1.0000 | 0.8889 | 0.1778 | 0.1502 | 0.7237 | 0.2098 | **79.733** |
| `dev_20260622_040201_gemma3_12b` | llm_rerank | 0.9778 | 0.7778 | 1.0000 | 0.8889 | 0.1883 | 0.1490 | 0.7237 | 0.1920 | 79.465 |

代理总分计算方式为：

```text
100 × (
  0.30 × mechanism_accuracy
  + 0.20 × strategy_score
  + 0.20 × risk_label_f1
  + 0.15 × response_reference_token_f1
  + 0.15 × format_score
)
```

### 5.1 Memory Router A/B

函数式动态路由只根据已确认的机制、策略、关系和风险语境过滤 ResponseSkill memory，不增加模型调用。`llm_rerank` 只允许从函数式候选 ID 中选择，不生成新规则或摘要。

对照结果显示，`llm_rerank` 的回复 F1 和总分低于函数式路由，同时增加了调用量和 prompt 成本。因此当前默认保留：

```python
MEMORY_ROUTER_MODE = "function"
```

### 5.2 最新本地规则改动

在最新代码中，StrategySkill 和 RiskSkill 做了小范围规则修正。基于上一轮 45 条 trace 的离线复算：

- Preferred Strategy Accuracy 从 `35/45` 提升到 `41/45`。
- Strategy Score 从 `0.8889` 估计提升到 `0.9556`。
- Risk F1 从 `0.7237` 估计提升到 `0.7548`。

该复算不等同于完整 LLM dev 运行，因为策略变化可能继续影响 ResponseSkill 的开放文本输出。正式结果需要重新执行完整 45 条 dev 后确认。

## 6. 当前限制

### 6.1 机制长尾混淆

剩余错误主要集中在：

- `achievement_drop / understated_flex`
- `understated_flex / faux_modesty`
- `understated_flex / self_aware_brag`
- `humble_complaint / understated_flex`
- `scarcity_flex / understated_flex`

这些边界依赖叙事重心、事件完成状态和表达方式。继续添加宽泛关键词可能破坏正确样本，因此当前版本不再扩大硬覆盖规则。

### 6.2 策略偏好存在单一参考答案效应

当前 Gemma3/function-router 运行的 Preferred Strategy Accuracy 为 `0.7778`，Acceptable Strategy Rate 为 `1.0000`。多数非首选策略仍可接受，说明部分差异来自单一 preferred 标签，而不是明显的社交错误。

进一步针对 dev 调整策略矩阵可能降低 acceptable 覆盖率，并增加隐藏 test 的泛化风险。当前版本保留较高可接受率，不继续按 dev 样本定制。

### 6.3 开放文本指标仍有波动

意图、反馈和回复采用 Token F1，与单一参考答案的词面重合高度相关。即使语义合理，不同模型的同义表达也会产生较大波动。当前流程通过短文本、原帖细节和 response few-shot 提高重合度，但不使用 dev 原句硬编码。

### 6.4 Embedding 未体现稳定收益

Embedding 可以改善同义表达召回，但当前数据规模较小，场景字段和最终策略对 response few-shot 更重要。已有对照未显示稳定优势，因此 Jaccard 继续作为默认方案，embedding 保留为可选能力。

## 7. 当前推荐配置

当前本地推荐模型是：

```env
OPENAI_MODEL=gemma3:12b
```

选择依据：

- 当前 Gemma3 12B 完整 dev 分数高于旧多模型基线。
- 机制准确率达到 `0.9778`。
- 函数式 memory router 比 llm rerank 更稳、更省调用。
- 格式检查通过，完整 dev 覆盖 45/45。

生成最终 test 时建议保持以下稳定配置：

```python
RUN_MODE = "test"
USE_MEMORY = True
USE_FEWSHOT = True
FEWSHOT_RETRIEVAL_MODE = "jaccard"
MEMORY_ROUTER_MODE = "function"
SAVE_ERROR_MEMORY = False
USE_GENERATED_MEMORY = False
```

执行命令：

```bash
python main.py
```

正式提交前以本次输出目录中的 `format_report.json` 为准，提交 `submission.jsonl`。公开 dev 代理分用于比较模型和检查回归，隐藏 test 仍包含独立的质量与鲁棒性评估。
